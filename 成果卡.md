# 成果卡（草稿 v1 · 2026-09-19）

> 按大赛「六、成果提交」模板起草，未定项标 ⏳。

## 基本信息

- **队伍名称**：⏳（待填）
- **任务编号与题目方向**：选题 8 · 把 AI 请进 SQL——opentenbase_ai 扩展实践（应用＋文档）
- **解决的问题（一句话）**：opentenbase_ai 扩展上游没有 README，用户无法独立完成"安装→注册模型→一条 SQL 完成 AI 任务"的闭环（Issue #304，PR #305/#306 均未合并）。
- **成果链接或附件**：⏳ PR 链接（提交后回填）｜附件：项目共享盘「opentenbase_ai-from-0-to-1」（README + 3 段 SQL）
- **初评版本**：⏳ commit 或提交文件版本

## 核心改动（最多三条）

1. **《opentenbase_ai 从 0 到 1》README**：安装前置（含 PG18 基线与本组 VM 环境复用）、模型注册（`ai_model_list` 复制表设计 + `json_path` 结果提取机制源码级讲解）、最小调用示例（摘要生成/文本分类）、FAQ、**已核对与待验证诚实清单**。
2. **三段配套 SQL**：`01_minimal_call`（一条 SQL 完成摘要/分类/批量加工）、`02_mock_httpbin`（与上游回归测试同源的 mock 用例，明确声明只验证请求构造/响应处理）、`03_rag_pgvector`（embedding 写入+余弦相似度检索，叠加本组 pgvector v0.8.6 的 `hnsw_recommend_ef_search`/`ivfflat_recommend_probes` 调参推荐）。
3. **源码级核对与差异化补充**：对照 master 逐文件核对函数名/参数/GUC/JSON path；发现 `ai.*` 函数默认 PUBLIC 可 EXECUTE 的安全问题并写入文档；针对 PR #305/#306 未覆盖的 mock 边界与 RAG 案例做补充而非另起炉灶。

## 修改前后对比 / 新增材料说明

- **前**：`contrib/opentenbase_ai/` 无 README.md（GitHub 目录可查）；Issue #304 挂起；PR #305（611 行）、PR #306（228 行）均 Open 未合并。
- **后**：本组 README 定位为"补充未覆盖内容"——mock 验证边界声明（两 PR 均未强调 httpbin 不能当真实推理）、pgvector RAG 案例、安全提示；对比截图 ⏳（PR 页面 + README 预览）。

## 验证与依据

- **参考资料及对应章节**：`contrib/opentenbase_ai/opentenbase_ai--1.0.sql`（全部函数与表结构）、`ai.c`（GUC 注册）、`sql/opentenbase_ai.sql`（回归测试用例）、`opentenbase_ai.control`（requires='http'）、Issue #304 / PR #305 / PR #306 状态（2026-09-19 查询）。
- **已完成的检查**：✅ 函数名/参数/默认值/GUC/JSON path 与 master 源码逐字核对（README §8 清单）；✅ 上游 Issue/PR 最新状态核查；✅ README 覆盖"完成一次调用所需信息"自查（安装→建扩展→注册→调用→排错→卸载）。
- **跨组试读反馈**：⏳（任务 r2DzCE，ling 交叉验证中）
- **未验证或仍需确认**：⏳ 真实环境中的编译安装/CREATE EXTENSION/mock 调用/真实模型调用/pgvector 检索（README §8 全部标注待实测，VM 192.168.35.128 就绪后回填）。

## 挑战与解决过程

1. **难点**：上游已有两个 README PR 并存，另起文档必然重复且不会被合并。**方案**：先查 PR #305/#306 状态与内容，定位差异化补充点（mock 边界声明、RAG 案例、安全提示），与现有 PR 互补。**证据**：PR files API 比对记录 + README §0 上游状态表。
2. **难点**：本组无 OpenTenBase 运行环境，无法现场实测。**方案**：① 所有函数级结论只做"源码级核对"并在文档中诚实标注待实测；② 复用本组犀牛鸟 VM（192.168.35.128, PG18.6, 已装增强 pgvector v0.8.6）作为实测环境，给出四步检测清单；③ 规避已知坑（PG18+pgvector0.8.6 建 HNSW >2 万行段错误，演示规模 ≤1 万行）。**证据**：README §8 清单 + 技术报告《向量索引构建与诊断增强》§8 复现范围与限制。

## 加分与工具使用证明

- **PR 链接、目标仓库及当前状态**：⏳ `Memsetqwq/OpenTenBase` 分支 `feat/opentenbase-ai` → 目标 `OpenTenBase/OpenTenBase:master`（提交后回填链接与 commit）
- **WorkBuddy 使用记录**：关键截图/日志 ⏳（本对话完整过程：源码抓取核对 → 文档生成 → 项目共享盘 4 文件上传 → 任务板协作与点名通知 → docx 技术报告读取 → 计划 v2 迭代；建议截取：任务板 3 任务、留言板通知、共享盘目录）
- **AI 辅助操作与人工核对**：AI 完成上游状态核查、源码比对、README/SQL 起草、跨成员任务分发；人工核对项：⏳ ling 源码交叉验证（r2DzCE）、老大确认共享盘覆盖上传
- **现场发布文章**：⏳ 平台/链接/发布时间（活动截止前必须发布可访问链接，否则影响力产出计 0）

---

## 附：评分对齐自查（大赛初评 100 分）

| 维度 | 分值 | 本组对应证据 | 状态 |
| --- | --- | --- | --- |
| 实际价值 | 15 | 解决"扩展无文档无法完成一次调用"真实使用问题；RAG 案例有应用价值 | ✅ 材料就绪 |
| 正确性与依据 | 15 | 源码逐字核对 + 参考章节标注 + 待验证项诚实清单 | ✅ |
| 完成度与验证 | 10 | 最低交付达成；实测待 VM 回填 | ⏳ |
| 贡献说明 | 5 | 本成果卡 + PR 描述 | ⏳ PR 待定稿 |
| 任务挑战度 | 15 | 跨源核对（源码/测试/PR 三方）；差异化定位；无环境下的诚实验证设计 | ✅ |
| 代码贡献-质量 | 10 | 文档贡献（与代码同权） | ✅ |
| 代码贡献-PR 提交 | 10 | **不提交 PR 计 0** → rsvHGq 是关键路径 | ⏳ 最高优先 |
| AI 工具深度 | 10 | WorkBuddy 日志/截图：生成→核对→上传→协作迭代 | ⏳ 截图待收集 |
| 影响力产出 | 10 | **截止前发布文章才计分**（0 或 3 + 质量 0-7） | ⏳ 最紧急 |
